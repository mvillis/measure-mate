#! /bin/sh

python manage.py migrate
python manage.py loaddata LAMA Governance DevOps tags

# won't work if there are already assessments, so ignore failures
python manage.py loaddata assessment || true

# won't work if there are already teams, so ignore failures
python manage.py loaddata team team_assessment || true
